اشتغل في المشروع الموجود `e:\e\omg\work crm` ونفّذ مهمة مطابقة التطبيق مع حزمة `OMG_Database_Fix_R1.zip` المرفقة.

حالة المشروع: Supabase السحابي غير مربوط، والهجرات لم تطبق عليه، والتطبيق لم ينشر. اختبار PostgreSQL المحلي المؤقت مسموح ومطلوب؛ تشغيل الاختبارات المرفقة لا يحتاج مفاتيح أو `.env.local` أو Docker. لا تطبق شيئًا على المشروع السحابي ولا تنشر التطبيق في هذه المهمة.

1. اقرأ README وREPORT وRPC_CONTRACT في الحزمة. افحص حالة ملفات المشروع واحفظ التعديلات الموجودة. انسخ ملفات `migrations/` الخمسة كما هي إلى `supabase/migrations/`، واستبدل ملفات V3.5 المحلية ذات الأسماء نفسها. لا تنشئ ملف migration سادسًا ولا تعِد توليد SQL.
2. احتفظ بباقي الحزمة في `tools/omg-db-verification/` بحيث يظل `migrations/` بجوار `verification/`. من `verification/` شغّل `npm ci --ignore-scripts` ثم `npm test`. يجب أن يعمل الاختبار محليًا بلا مفاتيح. اعرض الأمر، كود الخروج، والناتج الفعلي. إذا فشل، أصلح السبب المثبت دون حذف اختبارات أو تخفيف assertions، ثم أعد التشغيل.
3. طابق كود التطبيق الفعلي بالـ RPC_CONTRACT: أسماء المعاملات وترتيب أنواعها وقيم Enums ومفاتيح النتائج. استخدم استدعاءات RPC المسماة. طابق خدمات tasks/reviews/time/clients/campaigns/team/settings/reports والمرفقات والـ checklist والـ API routes والأنواع وأماكن الاستدعاء المتأثرة فقط. لا تعِد بناء الواجهة أو تغير توزيع العملاء.
4. أبقِ التفويض بهوية المستخدم. لا تستخدم service_role في متصفح أو كطريقة لتجاوز صلاحيات RPC. أرسل idempotency key جديدًا لكل عملية منطقية، واحتفظ بنفسه عند إعادة الطلب نفسه؛ تغيّر البيانات يعني عملية جديدة. إنشاء الدعوة يحتاج مفتاحًا غير فارغ؛ قبول الدعوة ذو استخدام واحد، والـ bootstrap للخادم فقط. ممنوع جعل غياب Supabase يعرض بيانات حية مزيفة.
5. شغّل اختبارات التطبيق والبناء الفعلي من جذر المشروع، وعالج أخطاء التوافق. أضف اختبار عقد لاستدعاءات التطبيق المتأثرة فقط عند الحاجة. صنّف اختبارات SQL المحلية منفصلة عن Auth/PostgREST/Storage HTTP والمتصفح المتصل. الأخيرة تظل غير منفذة حتى الربط الفعلي.
6. أخرج تقريرًا قصيرًا فيه الملفات المتغيرة ونتائج الأوامر الفعلية وما تبقى. سلّم ملف `app_alignment_changes.zip` يحتوي ملفات كود التطبيق والاختبارات المتغيرة وpackage.json وملف القفل والتقرير، بدون أسرار أو node_modules أو .next. الهدف أن أرسله للمراجعة، لا أن أرسل migrations فقط مرة أخرى.

معيار إنهاء هذه المهمة: اختبارات SQL المرفقة ناجحة + كود التطبيق مطابق للعقد + بناء التطبيق واختباراته ناجحة، مع إعلان أي فحص لم يُنفذ. نفّذ العمل ولا تكتفِ بخطة. لا توسّع المهمة إلى إعادة تصميم النظام أو دورة إصدارات جديدة.
